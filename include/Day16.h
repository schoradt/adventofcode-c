
#include "Base.h"

#include <map>
#include <string>

using namespace std;

class Day16: public Base
{
private:
    // private vars
public:
    Day16(/* args */);

    long process1(vector<string> input);

    long process2(vector<string> input);
};
